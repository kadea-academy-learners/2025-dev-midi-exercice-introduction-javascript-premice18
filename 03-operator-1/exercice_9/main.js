let paul, marie, eric, clair, madameMukuna, joseph, sarah;
// Ecrivez votre code ici
let maisonFamiliale = 60000000;
let terrains = 40000000;
let liquidites = 20000000;
let heritage = maisonFamiliale + terrains + liquidites;

paul = (heritage * 0.75) / 3;
marie = (heritage * 0.75) / 3;
eric = (heritage * 0.75) / 3 / 2;
clair = (heritage * 0.75) / 3 / 2;

madameMukuna = (heritage * 0.25) / 3;
joseph = (heritage * 0.25) / 3;
sarah = (heritage * 0.25) / 3;
// Ne pas modifier le code ci-dessous
module.exports = {
  paul,
  marie,
  eric,
  clair,
  madameMukuna,
  joseph,
  sarah,
};
